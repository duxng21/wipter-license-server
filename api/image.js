import {cors,decryptSecret,getLicense,normalizeKey,normalizeMachineId,requirePost,send,serverError,supabaseAdmin} from '../_lib.js';
export default async function handler(req,res){if(cors(req,res)||!requirePost(req,res))return;try{const key=normalizeKey(req.body?.key),machineId=normalizeMachineId(req.body?.machineId);if(!key||!machineId)return send(res,400,{ok:false,code:'invalid_request',error:'License key hoặc mã máy không hợp lệ.'});const db=supabaseAdmin(),license=await getLicense(db,key);if(!license||license.status!=='active'||license.machine_id!==machineId)return send(res,403,{ok:false,code:'license_invalid',error:'License không hợp lệ trên thiết bị này.'});const{data,error}=await db.from('app_images').select('*').eq('id',1).maybeSingle();if(error)throw error;if(!data?.enabled||!data.pull_url)return send(res,404,{ok:false,code:'image_unavailable',error:'Image chưa được cấu hình.'});return send(res,200,{ok:true,image:{pullUrl:data.pull_url,username:data.registry_username||'',token:decryptSecret(data.registry_token_encrypted||''),updatedAt:data.updated_at}})}catch(error){return serverError(res,error)}}
