import {cors,requireAdmin,send,serverError,supabaseAdmin} from '../_lib.js';
export default async function handler(req,res){if(cors(req,res)||!requireAdmin(req,res))return;try{const db=supabaseAdmin(),{data,error}=await db.from('app_images').select('pull_url,registry_username,registry_token_encrypted,enabled,updated_at').eq('id',1).maybeSingle();if(error)throw error;return send(res,200,{ok:true,image:{pullUrl:data?.pull_url||'',username:data?.registry_username||'',hasToken:Boolean(data?.registry_token_encrypted),enabled:Boolean(data?.enabled),updatedAt:data?.updated_at||''}})}catch(error){return serverError(res,error)}}
